## How to run the project

### Run the HTML/CSS/JS frontend

For this you just need to open the index.html file on your favourite browser, write the word you want to count and press the button.

### Run the GO backend

For this you need to open the terminal in the API folder then you can run the code by running the following command:

$ go run main.go